# Stage 3 Archive

This archive keeps old Stage 3 output layouts that are no longer part of the
current working structure.

- `legacy_flat_outputs/`
  Former flat directories that used to live directly under `outputs/stage3/`,
  such as `data/` and `baselines/`.

These files are kept only for backward reference and should not be used as the
current authoritative Stage 3 outputs.
