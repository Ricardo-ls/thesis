# stage3_canonical_v1

Fixed Stage 3 canonical evidence pipeline.

Run from repository root:

```bash
.venv/bin/python -m tools.stage3.canonical_v1.run_canonical_v1
```

This pipeline rebuilds the evidence layer from a single raw per-case source and writes all primary outputs under `outputs/stage3/canonical_v1/`.

